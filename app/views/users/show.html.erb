<%= link_to '新規メモ', new_note_path %>
<div>
  <div>
    <% @notes.each do |note|%>
      <div>
        <p>
          <%= link_to note.title, note_path(note.id) %>
        </p>
      </div>
    <%end%>
  </div>
</div>