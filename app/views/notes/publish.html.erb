<% @notes.each do |note| %>
  <% if note.released? %>
    <br>
    <%= link_to note.title, note_path(note.id) %>
    <br>
    <%note.tags.each do |tag| %>
      #<%= tag.name %>
      <br>
    <%end%>
    <%= note.user.nickname %>
    <br>
  <% end %>
<% end %>