<div class="wrapper">
  <div class="side_bar">
    <%= render "side_bar" %>
  </div>
  <div class="main">
    <%= form_with model: @note, url: "/notes", class:'form-wrap', local: true do |f| %>
      <%= render "main", f:f %>
    <% end %>
  </div>
</div>
